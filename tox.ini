[tox]
envlist = py{37}
requires = tox-venv
           setuptools

[pycodestyle]
ignore=E501
max-line-length = 500

[testenv]
deps = pylama
       -rrequirements.txt
       -cconstraints.txt

commands = pylama ghettogames
	   pylama scripts